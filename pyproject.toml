# This project was generated with 0.3.19 using template: https://github.com/beeware/briefcase-template@v0.3.19
[tool.briefcase]
project_name = "ilab"
bundle = "ai.instructlab"
version = "0.0.1"
url = "https://github.com/instructlab/instructlab"
license.file = "LICENSE"
author = "InstructLab Developers"
author_email = "dev@instructlab.ai"

[tool.briefcase.app.instructlab]
formal_name = "InstructLab"
description = "InstructLab Command-Line Interface"
long_description = """More details about the app should go here.
"""
sources = [
    "src/instructlab/src/instructlab",
]
test_sources = [
    "src/instructlab/tests",
]

console_app = true
requires = [
    "git+https://github.com/instructlab/instructlab.git@stable",
]
test_requires = [
    "pytest",
]

[tool.briefcase.app.instructlab.macOS]
universal_build = false
requires = [
]

[tool.briefcase.app.instructlab.linux]
requires = [
]

[tool.briefcase.app.instructlab.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.instructlab.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.instructlab.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.instructlab.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.instructlab.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "23.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.instructlab.windows]
requires = [
]

# Mobile deployments
[tool.briefcase.app.instructlab.iOS]
supported = false

[tool.briefcase.app.instructlab.android]
supported = false

# Web deployments
[tool.briefcase.app.instructlab.web]
supported = false

